# Development and testing dependencies
# Install with: pip install -r requirements-dev.txt
pytest==8.2.0
pytest-cov==5.0.0
httpx==0.27.0   # required by FastAPI TestClient in newer Starlette versions
