[pytest]
# Add the project root to sys.path so all modules (database, config, routes, etc.)
# are importable without needing to cd into the directory first.
# Required for CI where pytest is invoked from the repo checkout root.
pythonpath = .
testpaths = tests
